'use client';
import { AreaChart,Area,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid } from 'recharts';
import type { PricePoint } from '@/lib/types';
export function PriceChart({data,currency}:{data:PricePoint[];currency:string}){return <><p className="muted text-sm">近一個月 · 模擬走勢 · {currency}</p><div className="price-chart" role="img" aria-label={`模擬月線，起點 ${data[0]?.price}，終點 ${data.at(-1)?.price} ${currency}`}><ResponsiveContainer width="100%" height="100%"><AreaChart data={data} margin={{top:20,right:8,bottom:0,left:0}}><defs><linearGradient id="priceFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#4465df" stopOpacity={0.22}/><stop offset="100%" stopColor="#4465df" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#edf0f5" vertical={false}/><XAxis dataKey="label" tick={{fontSize:11}} tickLine={false} axisLine={false} minTickGap={35}/><YAxis domain={['auto','auto']} tick={{fontSize:11}} tickLine={false} axisLine={false} width={48}/><Tooltip formatter={value=>[`${value} ${currency}`,'模擬價格']}/><Area type="monotone" dataKey="price" stroke="#4465df" strokeWidth={2.5} fill="url(#priceFill)" isAnimationActive={false}/></AreaChart></ResponsiveContainer></div></>}
