'use client';
import { useState } from 'react';
export function Allocation({rows}:{rows:{market:string;type:string;value:number}[]}){const [mode,setMode]=useState<'market'|'type'>('market');const total=rows.reduce((a,r)=>a+r.value,0);const first=rows.filter(r=>mode==='market'?r.market==='TW':r.type==='etf').reduce((a,r)=>a+r.value,0);const p=total?first/total*100:0;return <><div className="segmented" aria-label="資產配置分類"><button aria-pressed={mode==='market'} onClick={()=>setMode('market')}>依市場</button><button aria-pressed={mode==='type'} onClick={()=>setMode('type')}>依資產類型</button></div><div className="allocation"><div className="donut" role="img" aria-label={`${mode==='market'?'台股':'ETF'} ${p.toFixed(1)}%，${mode==='market'?'美股':'個股'} ${(100-p).toFixed(1)}%`} style={{background:`conic-gradient(#4465df 0 ${p}%,#a8b8ed ${p}% 100%)`}}><div><small>資產分布</small><strong>100<span>%</span></strong></div></div><div className="allocation-legend"><p><i/>{mode==='market'?'台股':'ETF'}<strong>{p.toFixed(1)}%</strong></p><p><i/>{mode==='market'?'美股':'個股'}<strong>{(100-p).toFixed(1)}%</strong></p><small>依台幣換算市值計算</small></div></div></>}
